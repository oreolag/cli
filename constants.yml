---

#github
github:
  push_branch: my_workflows
  odev_repo: "https://github.com/oreolag/cli"

#paths
paths:
  cmdb: /opt
  models: "${HOME}/odev/models"
  projects: "${HOME}/odev/projects"
  tmp: /tmp
  workflows: "${HOME}/odev/workflows"